const MILLISECONDS = 1000;
const SECONDS = 60;
const MINUTES = 60;

const THIRTY_MINUTES = (MINUTES / 2) * SECONDS * MILLISECONDS;

function lookForUpdades() {}

setInterval(lookForUpdades, THIRTY_MINUTES);
